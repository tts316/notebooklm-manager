本文件是本系統的唯一權威規格文件，日後有調整請同步更新本文件對應章節。

# NotebookLM 管理工具 — 系統架構與規格

## 一、系統定位與執行平台

一句話：管理「哪些 Google NotebookLM 筆記本」要分享給「哪些使用者」（Viewer/Editor 權限），並產生可直接貼進 NotebookLM 分享框的 Email 清單。

- **技術棧**：Python 單檔 Streamlit App（`app.py`），資料層用 `gspread` 操作 Google Sheets 當資料庫。
- **部署方式**：Streamlit Community Cloud（依 `st.secrets["gcp_service_account"]` / `st.secrets["sheet_config"]` 取憑證的寫法判斷，非 Cloud Run／非 Docker；本 repo 內也沒有 Dockerfile 或 `.streamlit/` 設定檔）。
- **GitHub repo**：`tts316/notebooklm-manager`，分支 `main`，push 後由 Streamlit Cloud 自動重新部署。
- **正式網址**：未在 repo 內留下紀錄，需另外向使用者確認 Streamlit Cloud 上的服務網址並補充於此。
- **專案檔案**：整個 repo 只有 `app.py`（單檔含資料層＋UI）與 `requirements.txt` 兩個檔案，無子模組、無 utils/pages 目錄。

## 二、資料來源／資料庫架構

資料庫是一份 Google Sheets 試算表（URL 存於 `st.secrets["sheet_config"]["spreadsheet_url"]`），透過 gspread 讀寫，包含 3 個工作表：

### 1. `notebooks`（Notebook 主題清單）
由程式寫入的欄位（依 `upsert_notebook` 的 `append_row` 順序）：
| 欄位 | 說明 |
|---|---|
| notebook_id | NotebookLM 網址結尾的 ID（使用者可手動輸入，若留空則用當下時間戳記自動產生） |
| notebook_name | 主題名稱 |
| owner | 建立者（登入帳號） |
| 建立日期 | `YYYY-MM-DD` |

### 2. `permissions`（分享權限清單）
由程式寫入/讀取的欄位（對應 `upsert_permission`／`revoke_permission`／`batch_import` 的欄位順序與 `get_all_records()` 的 key 名稱）：
| 欄位 | 說明 |
|---|---|
| notebook_id | 對應 notebooks 的 ID |
| user_email | 分享對象 Email |
| role | `Viewer` 或 `Editor` |
| status | `Active` / `Revoked`（移除採軟刪除，不物理刪列） |
| 時間戳 | 新增或最後更新時間 |

### 3. `system_admin`（登入帳號密碼表）
- 第 1 欄：帳號（用 `find(username, in_column=1)` 查找）
- 第 2 欄：密碼（明碼比對，見下方「已知限制」）

### 憑證讀取方式
- Google Service Account 憑證：`st.secrets["gcp_service_account"]`（整包 dict）
- 試算表網址：`st.secrets["sheet_config"]["spreadsheet_url"]`
- 授權範圍：`spreadsheets` + `drive`
- 兩者皆存於 Streamlit Cloud 後台的 Secrets 設定，repo 內不含任何憑證或密碼明文。

## 三、角色與權限

系統本身只有「單一登入帳號體系」，沒有分角色：
- 登入時輸入帳號密碼，與 `system_admin` 工作表逐列比對（明碼）。
- 登入成功後以 `st.session_state.logged_in` / `user_id` 維持 session 內狀態，無 session 過期機制、無登出以外的權限差異。
- 所有登入成功的使用者對所有 Notebook 主題與分享名單都有完整存取／編輯權限（無資料範圍限制、無管理員/一般使用者之分）。
- 這裡的「Viewer / Editor」角色，指的是**NotebookLM 分享對象**的權限（分享出去的人是檢視者還是編輯者），與本系統的登入帳號權限無關。

## 四、主要功能模組

### 1. 登入頁（程式入口）
- 帳號密碼輸入 → `verify_login()` 比對 `system_admin` 工作表 → 成功後導向主畫面。

### 2. 側邊欄：主題選擇與建立
- 讀取 `notebooks` 工作表，以下拉選單選擇目前操作的 Notebook 主題。
- 若尚無主題，提示先建立。
- 「➕ 建立新主題」開啟 `st.dialog`（`show_add_notebook_dialog`）：輸入主題名稱與 Notebook ID（ID 留空則自動用時間戳記產生），呼叫 `upsert_notebook()` 寫入或更新（用 `find(nb_id)` 判斷新增或更新）。

### 3. 主畫面：分享名單總覽
- 顯示所選主題名稱、ID，並提供「🔗 前往 NotebookLM」直達連結（`https://notebooklm.google.com/notebook/{id}`）。
- 篩選該主題底下 `status == Active` 的分享名單，顯示：
  - 總分享人數、Editor 人數兩個 metric。
  - 用逗號＋空白組成的 Email 字串（`st.code` 呈現），方便直接複製貼到 NotebookLM 的分享框（NotebookLM 會自動辨識為多個聯絡人）。
  - 詳細表格：Email／權限（顏色標示，Editor 紅色、Viewer 綠色）／「移除」按鈕（呼叫 `revoke_permission()` 軟刪除，狀態改為 `Revoked`）。

### 4. 新增分享權限
兩種方式，皆寫入 `permissions` 工作表：
- **單筆新增**：輸入 Email＋選擇權限（Viewer/Editor）→ `upsert_permission()`。若該 Email 對該主題已有紀錄（含已 Revoked 的），會更新角色並把狀態重新設回 `Active`。
- **Excel 批次匯入**：提供範本下載（欄位 `Email`、`權限`），上傳 `.xlsx` → `batch_import()`。邏輯是「只新增資料庫中不存在的 (notebook_id, email) 組合」，已存在的不會覆蓋更新（與單筆新增的 upsert 行為不同，屬於既有實作行為，非猜測）。

## 五、已知限制／待辦事項

程式碼內沒有 TODO 註解，以下是通讀程式碼後觀察到、但目前仍是現況的技術限制：

- **密碼明碼儲存與比對**：`verify_login()` 直接以字串相等比對 `system_admin` 工作表內的明碼密碼，無雜湊、無加鹽。
- **無登入嘗試限制**：無鎖定、無速率限制。
- **無 session 逾時**：`st.session_state.logged_in` 只在使用者主動按「登出」或關閉分頁重整 session 時失效。
- **單一角色體系**：所有登入者權限相同，無法區分管理者與一般使用者。
- **批次匯入不覆蓋既有紀錄**：若同一 (notebook_id, Email) 已存在（含 Revoked 狀態），批次匯入會略過、不會重新啟用或更新角色；需用單筆新增才能重新啟用。
- **Notebook 主題無刪除功能**：只能新增/更新，UI 上沒有刪除主題的入口。
- **無自動化測試、無 CI**：repo 內僅有 `app.py` 與 `requirements.txt`。
- **正式部署網址未記錄**：需向使用者確認並補充於本文件「一、執行平台」章節。

## 六、變更歷程

| 日期 | commit | 主題 | 說明 |
|---|---|---|---|
| 2025-12-27 | `0df01ac` | 初版上傳 | 首次以「Add files via upload」方式建立 `app.py`（720 行）與 `requirements.txt`，奠定 Streamlit + gspread + Google Sheets 架構。 |
| 2025-12-27 | `8aa3134` | 大幅簡化重構 | `app.py` 從 811 行精簡為約 292 行（146 行增／665 行刪），移除大量早期版本的冗餘邏輯，收斂為現行的單頁式「登入→選主題→管理分享名單」架構。 |
| 2025-12-27 | `5754bd0` | 依賴清理 | `requirements.txt` 移除 3 個不再需要的套件，收斂為 streamlit／pandas／openpyxl／xlsxwriter／gspread／google-auth 六項。 |
| 2025-12-27 | `bf2c013` | 功能補強（現行版本） | 新增 Excel 範本下載＋批次匯入分頁、Email 清單一鍵複製格式修正（逗號後補空白以利 NotebookLM 辨識多聯絡人）、分享名單統計 metric、軟刪除（Revoked）機制等，形成目前 HEAD 版本的完整功能範圍。 |

| 2026-08-08 | `(本次)` | 文件 | 建立本規格文件首版，依現有程式碼與 git 歷史整理（此前 repo 內無任何 .md 說明文件）。 |
